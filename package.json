{
  "name": "relay-messaging",
  "version": "2.0.0",
  "private": true,
  "description": "Relay \u2014 an accessible messaging app: Node + SQLite server, zero runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test --test-concurrency=1 'test/**/*.test.js'",
    "reports": "node scripts/reports.js"
  }
}
